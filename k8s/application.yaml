apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: frontend-banka
  namespace: argocdfront
spec:
  project: default

  source:
    repoURL: 'https://github.com/rima-gif/frontBanka'
    targetRevision: HEAD
    path: k8s
  
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
